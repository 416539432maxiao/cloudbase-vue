# CoolTeacherPoint Skills 详细能力总结

精简后共 **11 个** Skill，覆盖 **Nuxt 3 前端 + 云函数 + 文档型数据库** 技术路线。

---

## 1. web-development — Web 前端开发规则

| 能力 | 说明 |
| :--- | :--- |
| 项目结构 | 源码 `src/`、构建 `dist/`、云函数 `cloudfunctions/` |
| 构建部署 | Vite 构建 → CloudBase 静态托管，hash 路由避免 404 |
| SDK 集成 | `@cloudbase/js-sdk` 初始化（同步、单例），调用数据库/云函数/认证 |
| 认证规范 | **必须用 SDK 内置认证**，禁止在云函数中实现登录逻辑 |
| 本地预览 | `npx live-server` 预览静态页面 |

---

## 2. ui-design — 专业 UI 设计指南

| 能力 | 说明 |
| :--- | :--- |
| 设计流程 | 用户体验分析 → 界面规划 → 美学方向 → 高保真设计 → 前端原型 → 真实感增强 |
| 强制设计规范 | 每次编码前必须输出完整 Design Specification（目的、美学方向、调色板、字体、布局策略） |
| 禁止项 | 禁用紫色系渐变、禁用 Inter/Roboto/Arial 等通用字体、禁用 Emoji 做图标、禁用纯居中对称布局 |
| 图标要求 | 只用专业图标库（FontAwesome / Heroicons / Lucide 等） |
| 自审清单 | 颜色审计 → 字体审计 → 图标审计 → 布局审计 → 规范合规检查 |

---

## 3. auth-web-cloudbase — Web 端认证

| 能力 | 说明 |
| :--- | :--- |
| 手机验证码登录 | `auth.signInWithOtp({ phone })` + `verifyOtp()` |
| 邮箱验证码登录 | `auth.signInWithOtp({ email })` + `verifyOtp()` |
| 密码登录 | 支持用户名/邮箱/手机号 + 密码 |
| 匿名登录 | `auth.signInAnonymously()` |
| OAuth 三方登录 | Google / 微信 等 OAuth 提供商 |
| 自定义票据登录 | `auth.signInWithCustomTicket()` |
| 用户管理 | 获取/更新用户信息、修改密码、重置密码、绑定/解绑三方、删除账号 |
| 状态监听 | `auth.onAuthStateChange()` 监听登录状态变化 |

---

## 4. auth-tool-cloudbase — 认证配置管理工具

| 能力 | 说明 |
| :--- | :--- |
| 查询登录策略 | `DescribeLoginStrategy` 查看当前已开启的登录方式 |
| 开关登录方式 | 匿名 / 用户名密码 / 短信 / 邮箱 / 微信 / Google |
| 短信配置 | 自定义短信验证配置（类型、频率限制等） |
| 邮箱配置 | 腾讯云邮件 或 自定义 SMTP（QQ邮箱等） |
| Publishable Key | 查询/创建 Web SDK 所需的公钥 |

---

## 5. auth-nodejs-cloudbase — 服务端认证（Node.js）

| 能力 | 说明 |
| :--- | :--- |
| 获取调用者身份 | `auth.getUserInfo()` → uid, openId, customUserId |
| 查询用户完整信息 | `auth.getEndUserInfo(uid)` 按 UID 查询 |
| 按登录标识查用户 | `auth.queryUserInfo({ platform, platformId })` 按手机/邮箱/用户名查 |
| 获取客户端 IP | `auth.getClientIP()` 用于安全审计 |
| 自定义登录票据 | `auth.createTicket(customUserId)` 桥接自有用户系统到 CloudBase |

---

## 6. cloudbase-document-database-web-sdk — 文档数据库（Web）

| 能力 | 说明 |
| :--- | :--- |
| 基础 CRUD | `doc().get()` / `add()` / `update()` / `remove()` |
| 复杂查询 | `_.gt / _.lt / _.in / _.nin` 等查询操作符，多条件组合 |
| 排序分页 | `orderBy()` / `skip()` / `limit()` / `field()` 链式调用 |
| 聚合查询 | 分组统计、管道操作、时间维度聚合 |
| 地理位置 | 附近搜索、区域查询、地理索引 |
| 实时数据库 | `watch()` 实时监听文档变化（聊天/协作场景） |
| 安全规则 | 读写权限配置（简单权限 / 自定义规则表达式） |

---

## 7. cloud-storage-web — 云存储（Web）

| 能力 | 说明 |
| :--- | :--- |
| 文件上传 | `uploadFile()` 支持进度回调 |
| 临时下载链接 | `getTempFileURL()` 生成有效期内的访问 URL |
| 文件删除 | `deleteFile()` 批量删除 |
| 文件下载 | `downloadFile()` 下载到浏览器 |
| CORS 配置 | 需在控制台添加安全域名避免跨域 |

---

## 8. cloud-functions — 云函数开发

| 能力 | 说明 |
| :--- | :--- |
| 事件函数 | `exports.main = async (event, context) => {}`，SDK/定时触发 |
| HTTP 函数 | Express/Flask 等 Web 框架，端口 9000，需 `scf_bootstrap` |
| 多语言支持 | HTTP 函数支持 Node.js / Python / Go / Java |
| SSE 流式输出 | 服务端推送（AI 聊天、进度更新） |
| WebSocket | 双向实时通信 |
| 定时触发器 | Cron 表达式配置定时任务 |
| 日志查询 | `getFunctionLogs` + `getFunctionLogDetail` 查看执行日志 |
| HTTP 访问 | 为云函数创建 HTTP 端点，支持自定义域名 |
| 环境变量 | 安全配置敏感参数，更新时需 merge 避免覆盖 |

---

## 9. cloudbase-guidelines — CloudBase 通用开发准则

| 能力 | 说明 |
| :--- | :--- |
| 开发导航 | Web / 小程序 / 原生 App 不同平台的 Skill 选用路线图 |
| MCP 配置 | 各 IDE（Cursor / Claude Code / VS Code 等）的 MCP 配置方法 |
| 核心行为规则 | 前端优先、优先用 SDK 直连数据库、后端先部署再预览 |
| 部署流程 | 后端部署 → 前端更新端点 → 构建前端 → 部署静态托管 → 更新文档 |
| 控制台入口 | 数据库 / 云函数 / 存储 / 认证 / 日志等所有控制台 URL 速查 |

---

## 10. cloudbase-platform — CloudBase 平台知识

| 能力 | 说明 |
| :--- | :--- |
| 存储 vs 托管 | 云存储（私密文件）vs 静态托管（公开网页）的区别与用法 |
| 认证最佳实践 | Web 用 SDK 认证、小程序天然免登录，严格区分平台 |
| 数据库权限 | 5 种权限模型（READONLY / PRIVATE / ADMINWRITE / ADMINONLY / CUSTOM）及适用场景 |
| 数据模型 | NoSQL 文档/MySQL 数据模型的 SDK 调用差异 |
| 控制台管理 | 所有 CloudBase 控制台页面 URL 模板 |

---

## 11. spec-workflow — 标准软件工程流程

| 能力 | 说明 |
| :--- | :--- |
| Phase 1: 需求文档 | EARS 语法（When/While/Shall）编写需求和验收标准 |
| Phase 2: 技术方案 | 架构设计、技术选型、数据库/接口设计、测试策略 |
| Phase 3: 任务拆分 | 将需求和方案拆解为可执行的任务清单 |
| Phase 4: 任务执行 | 独立执行、实时更新 `tasks.md` 状态、保证质量 |
| 核心原则 | 每阶段需用户确认后才进入下一阶段 |

---

> [!TIP]
> 这 11 个 Skill 形成了完整的开发闭环：**需求分析 (spec-workflow)** → **UI 设计 (ui-design)** → **前端开发 (web-development)** → **认证 (auth-web + auth-tool + auth-nodejs)** → **数据库 (document-database)** → **文件存储 (cloud-storage)** → **云函数 (cloud-functions)** → **平台管理 (guidelines + platform)**
